# 남양주 스마트주정차 — 보안 강화 기획서

> 작성일: 2026-05-29  
> 기준 버전: v1.1.0  
> 작성자: 보안 검토 (Claude Sonnet 4.6)  
> 분류: 내부 개발 문서

---

## 1. 현황 보안 진단 요약

v1.1.0 코드베이스를 대상으로 실시한 보안 점검 결과입니다.

### 안전한 항목 ✅

| 항목 | 근거 |
|------|------|
| Electron 핵심 보안 설정 | `contextIsolation: true`, `nodeIntegration: false`, `webSecurity: true` 올바르게 설정 |
| IPC 채널 격리 | `contextBridge`로 허용된 채널만 렌더러에 노출, 무분별한 채널 없음 |
| SQL 인젝션 방어 | `better-sqlite3` 파라미터 바인딩(`?`, `@param`) 일관 사용 |
| XSS 방어 | `escapeHtml()` 함수가 모든 동적 innerHTML 에 적용됨 |
| 인증 구조 | 비밀번호는 렌더러에 저장되지 않음, bcryptjs 해시 사용, IPC로만 검증 |
| 로컬 스토리지 | 테마·사이드바 너비 등 비민감 설정값만 저장 |

### 취약점 발견 항목 ❌⚠️

| 번호 | 항목 | 위험도 | 위치 |
|------|------|--------|------|
| V-01 | VWorld API 키 소스코드 하드코딩 | 🔴 HIGH | `renderer/index.html` 22번줄 |
| V-02 | 기본 비밀번호 `1234` 강제 변경 미적용 | 🟠 MEDIUM | `src/ipc/handlers.js` 23번줄 |
| V-03 | Content Security Policy (CSP) 미설정 | 🟠 MEDIUM | `main.js` |
| V-04 | 관리자 세션 무제한 — 타임아웃 없음 | 🟡 LOW-MED | `static/js/modules/auth.js` |
| V-05 | 로그인 실패 횟수 제한 없음 (브루트포스) | 🟡 LOW-MED | `src/ipc/handlers.js` |
| V-06 | 관리자 조작 감사 로그 없음 | 🟡 LOW | `src/ipc/handlers.js` |
| V-07 | 자동 업데이트 파일 서명 검증 미확인 | 🟡 LOW | `main.js` |

---

## 2. 취약점 상세 분석

---

### V-01 🔴 VWorld API 키 소스코드 하드코딩

**위험도**: HIGH

**현재 코드** (`renderer/index.html` 22번줄):
```html
<script src="https://map.vworld.kr/js/vworldMapInit.js.do?version=2.0&apiKey=C6357BB4-3419-3A99-BDF2-531C8939E646"></script>
```

**문제점**:
- API 키가 HTML 소스에 평문으로 노출됨
- GitHub 공개 저장소에 커밋 → 인터넷에 영구 노출
- 악의적 사용자가 키를 복사해 동일 키로 무제한 API 호출 가능
- API 할당량 초과 시 지도 기능 전체 마비

**공격 시나리오**:
```
1. GitHub 저장소 소스 코드 열람
2. API 키 복사 (C6357BB4-3419-3A99-BDF2-531C8939E646)
3. 자체 서비스에서 남양주시 키로 VWorld 무단 사용
4. 할당량 소진 → 남양주시 앱 지도 기능 불능
```

---

### V-02 🟠 기본 비밀번호 '1234' 강제 변경 미적용

**위험도**: MEDIUM

**현재 코드** (`src/ipc/handlers.js` 23번줄):
```javascript
const hash = bcrypt.hashSync('1234', 10);
// DB 비어있을 때 초기 비밀번호로 '1234' 설정
```

**문제점**:
- 새 설치 시 기본 비밀번호가 `1234`로 고정
- 비밀번호 변경을 강제하지 않음 → 운영 환경에서도 `1234` 유지 가능
- 로컬 앱이라도 물리적 접근 시 즉시 침입 가능

---

### V-03 🟠 Content Security Policy (CSP) 미설정

**위험도**: MEDIUM

**문제점**:
- Electron의 BrowserWindow에 CSP 헤더가 설정되지 않음
- 외부 스크립트 로드를 제한하는 정책 없음
- 향후 XSS 취약점 발생 시 방어선 없음

**현재 로드하는 외부 리소스**:
```
https://map.vworld.kr/js/vworldMapInit.js    (지도)
https://cdn.jsdelivr.net/.../chart.js         (차트)
https://cdn.jsdelivr.net/.../xlsx.full.min.js (엑셀)
https://cdn.jsdelivr.net/.../turf.min.js      (GIS)
https://cdn.jsdelivr.net/.../html2canvas.js   (캡처)
```

---

### V-04 🟡 관리자 세션 무제한

**위험도**: LOW-MEDIUM

**문제점**:
- 로그인 후 앱을 종료하지 않으면 세션이 영구 유지
- 자리를 비운 PC에서 제3자가 관리자 기능 접근 가능
- 비활동 시간 기반 자동 잠금 없음

---

### V-05 🟡 로그인 브루트포스 방어 없음

**위험도**: LOW-MEDIUM

**문제점**:
- `auth:verify` IPC 핸들러에 시도 횟수 제한 없음
- 짧은 시간 안에 무제한 비밀번호 시도 가능
- 로컬 앱이지만 스크립트 자동화 공격 가능

---

### V-06 🟡 관리자 조작 감사 로그 없음

**위험도**: LOW

**문제점**:
- CCTV 추가·수정·삭제, 단속 데이터 초기화 등 중요 조작에 대한 로그 없음
- 사고 발생 시 원인 추적 불가
- 내부 감사 또는 행정 감사 대응 불가

---

### V-07 🟡 자동 업데이트 파일 서명 검증

**위험도**: LOW

**현황**:
- `electron-updater`가 `latest.yml`의 SHA512 해시로 파일 무결성 검증 ✅
- GitHub Releases에서만 업데이트 수신 ✅
- 코드 서명 인증서(Code Signing Certificate) 적용 여부 미확인 ⚠️

**문제점**:
- Windows에서 코드 서명 없는 exe는 SmartScreen 경고 발생
- 중간자 공격(MITM) 시 서명 없는 악성 exe로 교체 이론적 가능

---

## 3. 페이즈별 구현 계획

---

### Phase 1 — 즉시 조치 (🔴 Critical)

> 목표: 외부 노출 위험 즉시 차단  
> 기간: 1~2일  
> 난이도: ★★☆☆☆

#### 1-1. API 키를 설정 파일로 분리 (V-01)

**현재 구조**:
```
renderer/index.html  ← API 키 하드코딩
```

**개선 구조**:
```
config.json (gitignore에 추가)  ← API 키 저장
main.js                          ← config.json 읽어서 렌더러에 전달 (IPC)
renderer/index.html              ← IPC로 키를 받아 동적으로 스크립트 로드
```

**구현 방식**:
```javascript
// main.js
ipcMain.handle('config:getApiKey', () => {
  const cfg = JSON.parse(fs.readFileSync(path.join(appPath, 'config.json'), 'utf8'));
  return cfg.vworldApiKey;
});

// preload.js에 추가
config: {
  getApiKey: () => ipcRenderer.invoke('config:getApiKey')
}

// renderer/index.html — VWorld 스크립트 동적 로드
const apiKey = await window.electronAPI.config.getApiKey();
const s = document.createElement('script');
s.src = `https://map.vworld.kr/js/vworldMapInit.js.do?version=2.0&apiKey=${apiKey}`;
document.head.appendChild(s);
```

**config.json** (`.gitignore`에 추가):
```json
{
  "vworldApiKey": "C6357BB4-3419-3A99-BDF2-531C8939E646"
}
```

> 기존 사용자용: 앱 설치 시 기본 config.json 번들, 갱신은 수동 교체

---

#### 1-2. GitHub 저장소에서 API 키 히스토리 정리 (V-01 후속)

API 키가 이미 커밋 히스토리에 존재하므로:

1. **VWorld 관리 콘솔에서 기존 키 폐기 → 신규 키 발급**
2. `git filter-repo` 또는 BFG Repo Cleaner로 히스토리에서 키 제거
3. GitHub에 force push

---

### Phase 2 — 단기 조치 (🟠 High)

> 목표: 인증 보안 강화 및 방어 계층 추가  
> 기간: 3~5일  
> 난이도: ★★★☆☆

#### 2-1. 초기 비밀번호 강제 변경 (V-02)

**구현**:
- DB에 `password_changed` 플래그 컬럼 추가
- 기본값 `0` → 로그인 시 `0`이면 비밀번호 변경 화면 강제 진입
- 변경 완료 후 `1`로 설정

```sql
ALTER TABLE app_config ADD COLUMN password_changed INTEGER DEFAULT 0;
```

```javascript
// 로그인 성공 후 체크
const changed = db.prepare("SELECT value FROM app_config WHERE key='password_changed'").get();
if (!changed || changed.value === '0') {
  // 비밀번호 변경 화면으로 강제 이동
  showForcePasswordChange();
}
```

**비밀번호 강도 검증 추가**:
```
최소 8자 이상
영문 + 숫자 조합 필수
연속 반복 문자 금지 (1111, aaaa 등)
```

---

#### 2-2. 로그인 시도 횟수 제한 (V-05)

**구현**: 메모리 기반 시도 카운터

```javascript
// handlers.js
const loginAttempts = { count: 0, lockedUntil: null };

ipcMain.handle('auth:verify', async (event, password) => {
  const now = Date.now();
  
  // 잠금 상태 확인
  if (loginAttempts.lockedUntil && now < loginAttempts.lockedUntil) {
    const remaining = Math.ceil((loginAttempts.lockedUntil - now) / 1000);
    return { success: false, error: `잠금 중 (${remaining}초 후 재시도)` };
  }
  
  const result = bcrypt.compareSync(password, storedHash);
  
  if (!result) {
    loginAttempts.count++;
    if (loginAttempts.count >= 5) {
      loginAttempts.lockedUntil = now + 5 * 60 * 1000; // 5분 잠금
      loginAttempts.count = 0;
    }
    return { success: false, remaining: 5 - loginAttempts.count };
  }
  
  loginAttempts.count = 0;
  loginAttempts.lockedUntil = null;
  return { success: true };
});
```

| 조건 | 조치 |
|------|------|
| 5회 연속 실패 | 5분 잠금 |
| 잠금 중 시도 | 남은 시간 표시, 처리 거부 |
| 성공 시 | 카운터 초기화 |

---

#### 2-3. Content Security Policy 설정 (V-03)

`main.js`에서 세션 기본 헤더 설정:

```javascript
const { session } = require('electron');

app.whenReady().then(() => {
  session.defaultSession.webRequest.onHeadersReceived((details, callback) => {
    callback({
      responseHeaders: {
        ...details.responseHeaders,
        'Content-Security-Policy': [
          "default-src 'self';" +
          "script-src 'self' 'unsafe-inline' https://map.vworld.kr https://cdn.jsdelivr.net;" +
          "style-src 'self' 'unsafe-inline';" +
          "img-src 'self' data: https: blob:;" +
          "connect-src 'self' https://api.vworld.kr https://apis.data.go.kr https://map.vworld.kr https://uploads.github.com https://api.github.com;" +
          "font-src 'self' data:;" +
          "frame-src 'none';" +
          "object-src 'none';"
        ]
      }
    });
  });
});
```

> `'unsafe-inline'` 은 현재 인라인 스크립트가 많아 단기적으로 유지, Phase 3에서 nonce 방식으로 대체 검토

---

### Phase 3 — 중기 조치 (🟡 Medium)

> 목표: 운영 내구성 및 감사 추적성 확보  
> 기간: 1~2주  
> 난이도: ★★★★☆

#### 3-1. 관리자 세션 자동 잠금 (V-04)

**구현**: 비활동 시간 측정 + 자동 로그아웃

```javascript
// renderer/index.html
const SESSION_TIMEOUT_MS = 30 * 60 * 1000; // 30분
let lastActivity = Date.now();

const resetTimer = () => { lastActivity = Date.now(); };
['mousemove','keydown','click','scroll'].forEach(evt =>
  document.addEventListener(evt, resetTimer, { passive: true })
);

setInterval(() => {
  if (Date.now() - lastActivity > SESSION_TIMEOUT_MS) {
    // 로그인 화면으로 복귀
    Auth.logout();
    showToast('장시간 미사용으로 자동 잠금되었습니다.', 'warning');
  }
}, 60 * 1000); // 1분마다 체크
```

**설정 탭에 타임아웃 시간 조절 옵션 추가** (15분 / 30분 / 60분 / 사용 안 함)

---

#### 3-2. 관리자 조작 감사 로그 (V-06)

**기록 대상 조작**:

| 조작 | 기록 내용 |
|------|----------|
| CCTV 추가 | 추가된 CCTV명, 주소, 좌표, 시각 |
| CCTV 수정 | 변경 전/후 항목, 시각 |
| CCTV 삭제 | 삭제된 CCTV ID, 명칭, 시각 |
| 단속 데이터 초기화 | 초기화 전 건수, 시각 |
| CCTV 데이터 초기화 | 초기화 전 건수, 시각 |
| 비밀번호 변경 | 변경 시각 (비밀번호 내용은 미기록) |
| Excel 가져오기 | 파일명, 가져온 건수, 시각 |

**DB 구조 추가**:
```sql
CREATE TABLE IF NOT EXISTS audit_log (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  action      TEXT NOT NULL,   -- 'CCTV_ADD', 'CCTV_DELETE', 'RESET_ENFORCEMENT' ...
  detail      TEXT,            -- JSON 형식 상세 정보
  created_at  TEXT DEFAULT (datetime('now','localtime'))
);
```

**기존 로그 파일과 별개로** DB 내 테이블로 관리하여 내보내기·감사 대응 가능

---

#### 3-3. 입력값 길이 및 형식 검증 강화

현재 CCTV 추가/수정 시 입력값 검증이 프론트엔드에만 의존합니다.  
IPC 핸들러에 서버 측 검증 레이어 추가:

```javascript
function validateCCTVInput(data) {
  if (!data.cctv_name || typeof data.cctv_name !== 'string') throw new Error('CCTV명 필수');
  if (data.cctv_name.length > 100) throw new Error('CCTV명 100자 초과');
  if (typeof data.latitude !== 'number' || data.latitude < 37 || data.latitude > 38.5)
    throw new Error('위도 범위 오류 (남양주시 범위 초과)');
  if (typeof data.longitude !== 'number' || data.longitude < 126.5 || data.longitude > 128)
    throw new Error('경도 범위 오류 (남양주시 범위 초과)');
}
```

---

### Phase 4 — 장기 조치 (🟢 Enhancement)

> 목표: 엔터프라이즈 수준 보안 내구성  
> 기간: 1개월 이상  
> 난이도: ★★★★★

#### 4-1. 코드 서명 인증서 적용 (V-07)

Windows 코드 서명 인증서(EV Code Signing Certificate) 적용:
- SmartScreen 경고 제거
- 자동 업데이트 파일의 신뢰성 보장
- `electron-builder` `win.sign` 설정으로 빌드 시 자동 서명

```json
// package.json build 설정
"win": {
  "certificateFile": "cert.pfx",
  "certificatePassword": "${env.CERT_PASSWORD}"
}
```

> 인증서 비용: 연 10~50만원 (Sectigo, DigiCert 등)

---

#### 4-2. 민감 데이터 암호화 (DB)

현재 SQLite DB가 평문으로 저장됩니다.  
`better-sqlite3` + `SQLCipher` 확장으로 DB 파일 자체 암호화 검토:

```
%APPDATA%\namyangju-smart-parking\data\app.db
→ AES-256으로 암호화된 상태로 저장
→ 앱 실행 시 고정 키 또는 비밀번호로 복호화
```

> 단, SQLCipher는 네이티브 빌드가 필요하여 배포 복잡도가 높아짐

---

#### 4-3. CSP nonce 방식 전환 (V-03 고도화)

Phase 2에서 적용한 `'unsafe-inline'` 허용을 제거하고  
매 요청마다 새 nonce를 생성하여 인라인 스크립트를 제한:

```javascript
// main.js — nonce 기반 CSP
const nonce = crypto.randomBytes(16).toString('base64');
// BrowserWindow 로드 시 nonce를 렌더러에 전달
// HTML의 <script> 태그에 nonce 속성 추가
```

---

## 4. 페이즈 요약표

| Phase | 항목 | 대상 취약점 | 난이도 | 우선순위 |
|-------|------|------------|--------|---------|
| **Phase 1** | API 키 분리 + 키 갱신 | V-01 | ★★☆☆☆ | 🔴 즉시 |
| **Phase 2-A** | 기본 비밀번호 강제 변경 | V-02 | ★★☆☆☆ | 🟠 단기 |
| **Phase 2-B** | 로그인 횟수 제한 | V-05 | ★★☆☆☆ | 🟠 단기 |
| **Phase 2-C** | CSP 헤더 설정 | V-03 | ★★★☆☆ | 🟠 단기 |
| **Phase 3-A** | 세션 자동 잠금 | V-04 | ★★★☆☆ | 🟡 중기 |
| **Phase 3-B** | 감사 로그 | V-06 | ★★★☆☆ | 🟡 중기 |
| **Phase 3-C** | 입력값 서버 검증 | — | ★★☆☆☆ | 🟡 중기 |
| **Phase 4-A** | 코드 서명 인증서 | V-07 | ★★★★☆ | 🟢 장기 |
| **Phase 4-B** | DB 암호화 | — | ★★★★★ | 🟢 장기 |
| **Phase 4-C** | CSP nonce 방식 | V-03 고도화 | ★★★★☆ | 🟢 장기 |

---

## 5. 기존 보안 자산 (현재 잘 구현된 항목)

이미 잘 설계된 부분은 변경 없이 유지합니다.

| 항목 | 내용 |
|------|------|
| IPC 격리 | contextBridge 화이트리스트 방식, 렌더러→메인 단방향 제어 |
| SQL 인젝션 차단 | 모든 쿼리 파라미터 바인딩 적용 |
| XSS 차단 | `escapeHtml()` 일관 적용 (5개 엔티티 변환) |
| bcrypt 인증 | saltRounds=10, 평문 비밀번호 미저장 |
| 파일 다이얼로그 | 사용자가 직접 경로 선택, 임의 경로 접근 불가 |
| 앱 로그 | 30일 자동 삭제, 민감 데이터 미기록 |

---

## 6. 참고 기준

- [OWASP Desktop App Security Checklist v1.0](https://owasp.org/www-project-desktop-app-security-top-10/)
- [Electron Security Documentation](https://www.electronjs.org/docs/latest/tutorial/security)
- [KISA 전자정부 보안 가이드라인 2024](https://www.kisa.or.kr/)
- NIST SP 800-63B (비밀번호 정책 기준)

---

*이 기획서는 구현 착수 전 검토용이며, Phase 착수 전 실제 코드 상태를 재확인한다.*  
*보안 취약점 특성상 구체적 구현 코드는 이 문서에서 상세화를 최소화한다.*
